<%= render 'shared/searchbar' %>

<% if params[:query].present? %>
  <h1>Here are the tracks listed as <%= params[:query] %></h1>
<% else %>
  <h1>This is the opinion's index</h1>
<% end %>



<%= render 'shared/opinions-cards',
opinions: @opinions %>
