container = document.querySelector("[data-track=\"<%= @vote.track.id %>\"]");
inner = `<%= j render 'votes/heart', track: @vote.track %>`;
container.innerHTML = inner;

event = new Event('voteUpdated');

// Dispatch the event.
document.dispatchEvent(event);
